pragma solidity ^0.7;

contract main
{
 
 
 
 
    function op1 ( ) public {
      require(msg_value>x1);
      payable(msg.sender).transfer(amount);
      if ( x1>3 ){
        x1 = x1++;
      }      
      x1 = 33;
    }
    
}
