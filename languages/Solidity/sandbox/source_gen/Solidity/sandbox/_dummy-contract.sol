contract_dummy
{
    uint private nn;
    mapping (address => uint) private usersBalance;
    struct struct_definition {
      boolean check;
      uint amount;
      address user
    }
 
    enum name{elem1, elem2, elem3 }
 
    constructor (){
      nn= 565;
      nn= 546;
      if ( msg.value>2 ){
        nn= 100;
      }
      else {
        nn= 5651;

      }
}
  